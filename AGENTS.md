# AGENTS.md — Working Instructions for Codex

**Read [`CLAUDE.md`](CLAUDE.md) in the repo root before doing any work.**

It is the authoritative working-instructions file for every coding agent in
this repository, and it applies to you in full — the documentation-currency
rules, the four-place version lockstep, the load-bearing decisions that need a
question before they change, the WordPress and Python conventions, and the
six-point definition of "done."

Where it addresses "Claude Code," read "Codex." Nothing else in it is
agent-specific.

---

This file is deliberately a pointer rather than a copy. It previously
duplicated all ~210 lines of `CLAUDE.md`, which meant every edit had to be
made twice and the two files had already begun to drift. Keep the conventions
in one place: **make changes in `CLAUDE.md`, never here.**
